// Production values are a placeholder for now — real deployment config (e.g. a
// same-origin reverse-proxy path, or a built-time-injected URL) is Phase 4 (DevOps).
export const environment = {
  production: true,
  apiOrigin: '',
  apiBaseUrl: '/api',
};
