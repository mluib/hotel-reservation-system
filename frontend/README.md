# Frontend (placeholder)

The Angular application for this project has not been scaffolded yet.

Planned sequencing (see [docs/ROADMAP.md](../docs/ROADMAP.md)):
1. UX/design pass (screens and flows) before writing any Angular code.
2. A backend contract pass informed by that design.
3. Angular scaffold and implementation against the stabilized API.

This folder exists now so the repo's top-level structure (`backend/`, `frontend/`, `docs/`) is in place ahead of that work.
